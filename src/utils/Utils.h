#ifndef IRC_UTILS_H
#define IRC_UTILS_H

#include <string>
#include <vector>

class Utils {

public:
    static std::string join_vector(std::vector<int>);

    static int max(int, int);
};


#endif //IRC_UTILS_H
